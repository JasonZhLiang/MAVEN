package com.linguaclassica.repository;

import javax.persistence.EntityManager;
import javax.persistence.PersistenceContext;
//import javax.persistence.Query;

import org.springframework.stereotype.Repository;

@Repository
public class MetricGenRepository{
	
	@PersistenceContext
	private EntityManager entityManager;	
	/*
	public void callMonthlyMetrics() {
		Query query = entityManager.createNativeQuery("call monthly_metrics");
		int deleted = query.executeUpdate();
		System.out.println("After calling stored procedure!");
	}
*/
}
     
